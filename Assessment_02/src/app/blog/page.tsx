import { Metadata } from "next"

export const metadata: Metadata = {
  title: {
    absolute: "Blog",
  },
}

export default function Blog() {
  return (
    <div>
       <h2>My Blog</h2>
    </div>
  )
}
